# QuickRefurbz Configuration

# Database Type: sqlite (default, local dev) or postgres (production)
DB_TYPE=sqlite

# Data directory for SQLite database (default: ./data)
DATA_DIR=./data

# PostgreSQL Connection (only needed if DB_TYPE=postgres)
PGHOST=localhost
PGPORT=5432
PGDATABASE=quickrefurbz
PGUSER=postgres
PGPASSWORD=your_password_here

# Server port (for future API)
PORT=3001

# Photo Storage (local or s3)
PHOTO_STORAGE_MODE=local
# S3 Configuration (for Hetzner Object Storage or any S3-compatible service)
# S3_ENDPOINT=https://fsn1.your-objectstorage.com
# S3_BUCKET=quickrefurbz-photos
# S3_ACCESS_KEY_ID=your_access_key
# S3_SECRET_ACCESS_KEY=your_secret_key
# S3_REGION=fsn1
OPENAI_API_KEY=sk-your-openai-key-here

# Sourcing Database (read-only connection to upscaled_cogs on HetznerCO)
SOURCING_DB_HOST=5.161.239.237
SOURCING_DB_PORT=5432
SOURCING_DB_NAME=upscaled_cogs
SOURCING_DB_USER=your_sourcing_db_user
SOURCING_DB_PASSWORD=your_sourcing_db_password
